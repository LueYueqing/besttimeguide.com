# 辅助工具与自动化

基于youzikuaibao.com.cn项目，整理的完整自动化工具集和使用方法。

## 🛠️ 自动化工具生态

```
项目根目录/
├── automation-tools/               # 通用自动化工具
│   ├── backlink-publisher/         # 外链自动发布
│   ├── comment-template-generator/ # 评论模板生成
│   ├── seo-analyzer/               # SEO分析工具
│   ├── google-trends/              # Google Trends数据采集
│   ├── toolify-ai-scraper/         # 竞争对手采集
│   └── shared/                     # 共享配置和认证
└── tools/                          # 项目专用工具
    ├── preview-generator/          # 预览图生成工具
    └── favicon-generator/          # 图标生成工具
```

## 🎨 项目专用工具（基于whitescreen.show经验）

### 预览图生成工具
**位置**: `tools/preview-generator/`

**功能特点**:
- 支持17种不同屏幕尺寸
- 自动生成PNG和WebP格式
- 批量处理所有页面
- 智能命名和分类

**技术实现**:
```python
# 核心功能
- 多尺寸适配（xs, sm, md, lg, xl）
- 格式转换（PNG → WebP）
- 批量处理（所有屏幕效果）
- 质量优化（压缩和优化）
```

**使用场景**:
- 新页面上线前
- 定期更新预览图质量
- 社交媒体分享图片
- SEO优化图片素材

### Favicon生成工具
**位置**: `tools/favicon-generator/`

**功能特点**:
- 将WebP预览图嵌入显示器SVG模板
- 生成多尺寸ICO文件
- 支持高质量和简化版两种模式
- 自动适配不同屏幕效果

**技术实现**:
```python
# 核心功能
- SVG模板系统
- 图像嵌入和缩放
- 多格式输出（ICO, PNG, SVG）
- 批量处理
```

**使用场景**:
- 网站图标生成
- 浏览器标签页图标
- 移动端书签图标
- 社交媒体分享图标

## 📦 1. 外链自动发布工具

### 功能说明
自动在WordPress和其他网站上发布评论，建立外链，提升网站权威性。

### 目录结构
```
backlink-publisher/
├── backlink_automation.py      # 主程序
├── backlinks_api_client.py     # API客户端
├── wordpress_core.py           # WordPress核心功能
├── wordpress_comment_manager.py # 评论管理
├── wordpress_comment_submitter.py # 评论提交
├── auth_manager.py             # 认证管理
├── requirements.txt            # Python依赖
└── 使用说明.md
```

### 快速开始
```bash
# 1. 安装依赖
cd automation-tools/backlink-publisher
pip install -r requirements.txt

# 2. 配置认证
python auth_manager.py
# 按提示输入用户名和密码

# 3. 运行自动化
python backlink_automation.py
```

### 核心功能
```python
from backlink_automation import BacklinkAutomation

# 初始化
automation = BacklinkAutomation("http://your-server.com")

# 获取待处理目标
targets = automation.get_pending_targets(limit=10)

# 处理每个目标
for target in targets:
    result = automation.process_target(target)
    print(f"处理结果: {result}")
```

### 配置说明
```json
{
  "api_server": "http://localhost:3000",
  "max_retry": 3,
  "timeout": 30,
  "user_agent": "Mozilla/5.0...",
  "comment_templates": {
    "general": [
      "Great tool! Very helpful.",
      "Thanks for sharing this resource."
    ]
  }
}
```

### 最佳实践
```
✅ 每天处理10-20个目标（避免被识别为spam）
✅ 使用多样化的评论模板
✅ 设置随机延迟（30-60秒）
✅ 定期检查评论状态
✅ 保持评论质量和相关性

❌ 避免批量发送相同评论
❌ 避免短时间内大量提交
❌ 避免完全无关的评论
```

## 📝 2. 评论模板生成器

### 功能说明
自动采集网站页面，使用AI生成针对性评论模板，自动插入数据库。

### 目录结构
```
comment-template-generator/
├── scrape_website.py           # 🚀 主程序：网站采集
├── regenerate_comments.py      # 🚀 主程序：重新生成
├── core/
│   ├── universal_website_scraper.py  # 采集核心类
│   ├── regenerate_templates.py       # 重新生成核心类
│   ├── database_client.py            # 数据库客户端
│   └── database_config.py            # 数据库配置
├── scraper_config.json         # 配置文件
└── 快速开始.md
```

### 快速开始
```bash
cd automation-tools/comment-template-generator

# 1. 采集新网站
python scrape_website.py https://example.com

# 2. 重新生成已用完的模板
python regenerate_comments.py
```

### 配置文件（scraper_config.json）
```json
{
  "api_config": {
    "base_url": "http://localhost:3000",
    "api_key": "your-api-key-here"
  },
  "database_config": {
    "host": "localhost",
    "port": 3306,
    "user": "root",
    "password": "your-password",
    "database": "your_database"
  },
  "scraper_config": {
    "max_pages": 100,
    "max_depth": 3,
    "timeout": 30,
    "user_agent": "Mozilla/5.0...",
    "exclude_patterns": [
      "/admin/",
      "/login/",
      "/api/"
    ]
  },
  "comment_config": {
    "templates_per_page": 5,
    "max_usage_count": 3,
    "min_length": 50,
    "max_length": 200
  }
}
```

### 工作流程
```
1. 网站采集
   ↓
2. 页面内容分析
   ↓
3. AI生成评论模板
   ↓
4. 质量检查和过滤
   ↓
5. 插入数据库
```

### 评论模板示例
```python
{
  "url": "https://example.com/pdf-converter",
  "templates": [
    {
      "content": "This PDF converter is exactly what I needed! Works perfectly and it's free. Highly recommend it.",
      "category": "positive",
      "usage_count": 0,
      "max_usage": 3
    },
    {
      "content": "Great tool! I've been using it for a week and it handles all my PDF conversion needs efficiently.",
      "category": "recommendation",
      "usage_count": 0,
      "max_usage": 3
    }
  ]
}
```

### 最佳实践
```
✅ 每个页面生成5-10个不同模板
✅ 设置最大使用次数（3-5次）
✅ 定期重新生成已用完的模板
✅ 评论长度控制在50-200字符
✅ 保持评论自然和相关性

❌ 避免生成通用模板
❌ 避免重复使用相同模板
❌ 避免过长或过短的评论
```

## 🔍 3. SEO分析工具

### 功能说明
自动分析网站SEO问题，生成优化建议和报告。

### 目录结构
```
seo-analyzer/
├── seo_analyzer.py             # SEO分析器
├── seo_checker.py              # SEO检查器
├── quick_start.py              # 快速开始
├── website_crawler.py          # 网站爬虫
└── 使用说明.md
```

### 快速开始
```bash
cd automation-tools/seo-analyzer

# 分析单个页面
python quick_start.py https://example.com

# 分析整个网站
python seo_analyzer.py https://example.com --full-site
```

### 检查项目
```
✅ 标题优化（Title Tag）
✅ Meta描述（Description）
✅ H1-H6标题结构
✅ 图片Alt标签
✅ 内部链接
✅ 页面加载速度
✅ 移动端友好性
✅ 结构化数据
✅ robots.txt和sitemap.xml
✅ HTTPS配置
```

### 报告示例
```
SEO分析报告
============
网站: https://example.com
分析时间: 2025-01-16 10:00:00

总体评分: 78/100

优化建议:
1. [高优先级] 标题太长（67字符），建议控制在60字符内
2. [中优先级] 缺少meta description
3. [中优先级] 5张图片缺少alt标签
4. [低优先级] 页面加载时间3.2秒，建议优化至<3秒

详细报告: report_2025-01-16.html
```

## 📊 4. Google Trends数据采集

### 功能说明
自动采集Google Trends数据，分析关键词趋势。

### 快速开始
```bash
cd automation-tools/google-trends

# 单个关键词
python collect_trends.py "pdf converter"

# 批量关键词
python batch_collect.py keywords.txt
```

### 数据输出
```json
{
  "keyword": "pdf converter",
  "timeframe": "today 3-m",
  "data": [
    {"date": "2025-01-01", "value": 85},
    {"date": "2025-01-02", "value": 87},
    {"date": "2025-01-03", "value": 90}
  ],
  "related_queries": [
    {"query": "pdf to word", "value": 100},
    {"query": "online pdf converter", "value": 85}
  ],
  "rising_queries": [
    {"query": "free pdf converter", "value": "+150%"}
  ]
}
```

## 🕷️ 5. 竞争对手数据采集

### 功能说明
自动采集竞争对手的工具列表、功能、定价等信息。

### 目录结构
```
toolify-ai-scraper/
├── toolify_scraper_comprehensive.py  # 🌟 完整版
├── toolify_scraper_simple.py        # 简化版
├── toolify_scraper_selenium.py      # Selenium版
└── 项目总结.md
```

### 快速开始
```bash
cd automation-tools/toolify-ai-scraper

# 使用完整版采集器
python toolify_scraper_comprehensive.py

# 输出: toolify_tools_YYYY-MM-DD.json
```

### 采集数据示例
```json
{
  "tools": [
    {
      "name": "Sora AI",
      "url": "https://sora.ai?utm_source=toolify",
      "category": "video generation",
      "description": "AI video generator",
      "pricing": "free trial",
      "features": ["text to video", "high quality"]
    }
  ],
  "total_count": 139,
  "collected_at": "2025-01-16 10:00:00"
}
```

### 竞争分析流程
```
1. 采集竞争对手工具列表
   ↓
2. 分析功能和定价
   ↓
3. 找到差异化机会
   ↓
4. 制定产品策略
```

## 🔄 6. 自动化工作流

### 每日自动化任务
```bash
#!/bin/bash
# daily_automation.sh

# 1. 外链发布（每天10-20个）
cd automation-tools/backlink-publisher
python backlink_automation.py --limit 15

# 2. 重新生成评论模板
cd ../comment-template-generator
python regenerate_comments.py

# 3. SEO检查（每周一次）
if [ $(date +%u) -eq 1 ]; then
  cd ../seo-analyzer
  python seo_analyzer.py https://your-site.com --full-site
fi

# 4. 数据备份
cd ../..
./scripts/backup-database.sh
```

### 使用Cron定时执行
```bash
# 编辑crontab
crontab -e

# 每天凌晨2点执行
0 2 * * * /path/to/daily_automation.sh

# 每周一上午10点执行SEO分析
0 10 * * 1 /path/to/seo_analyzer.sh
```

### Windows定时任务
```batch
REM daily_automation.bat

@echo off
cd E:\AliYunPan\最近项目\youzikuaibao.com.cn\automation-tools

REM 外链发布
cd backlink-publisher
python backlink_automation.py --limit 15

REM 评论模板
cd ..\comment-template-generator
python regenerate_comments.py

echo 自动化任务完成！
```

## 📋 7. 共享配置管理

### 认证管理（shared/auth_manager.py）
```python
from auth_manager import AuthManager

# 初始化认证管理器
auth = AuthManager()

# 登录并保存token
auth.login("username", "password")

# 获取token（自动刷新）
token = auth.get_token()

# 检查认证状态
if auth.is_authenticated():
    print("已认证")
```

### 配置管理（shared/config.py）
```python
from config import Config

# 加载配置
config = Config()

# 获取配置值
api_url = config.get("api_server.base_url")
timeout = config.get("api_server.timeout", default=30)

# 更新配置
config.set("api_server.base_url", "https://new-url.com")
config.save()
```

## 🛡️ 安全和最佳实践

### 凭证管理
```
✅ 使用环境变量存储敏感信息
✅ 不要将凭证提交到Git
✅ 使用.gitignore忽略配置文件
✅ 定期更换API密钥
✅ 使用最小权限原则

❌ 避免硬编码密码
❌ 避免在日志中输出敏感信息
```

### 速率限制
```python
import time
from functools import wraps

def rate_limit(calls_per_minute=60):
    """速率限制装饰器"""
    min_interval = 60.0 / calls_per_minute
    last_called = [0.0]
    
    def decorator(func):
        @wraps(func)
        def wrapper(*args, **kwargs):
            elapsed = time.time() - last_called[0]
            left_to_wait = min_interval - elapsed
            if left_to_wait > 0:
                time.sleep(left_to_wait)
            result = func(*args, **kwargs)
            last_called[0] = time.time()
            return result
        return wrapper
    return decorator

@rate_limit(calls_per_minute=30)
def api_call():
    # API调用
    pass
```

### 错误处理和重试
```python
import time
from typing import Callable, Any

def retry_on_failure(
    max_retries: int = 3,
    delay: float = 1.0,
    backoff: float = 2.0
):
    """失败重试装饰器"""
    def decorator(func: Callable) -> Callable:
        def wrapper(*args, **kwargs) -> Any:
            retries = 0
            current_delay = delay
            
            while retries < max_retries:
                try:
                    return func(*args, **kwargs)
                except Exception as e:
                    retries += 1
                    if retries >= max_retries:
                        raise
                    
                    print(f"尝试 {retries}/{max_retries} 失败: {e}")
                    time.sleep(current_delay)
                    current_delay *= backoff
            
        return wrapper
    return decorator

@retry_on_failure(max_retries=3, delay=2.0)
def unreliable_api_call():
    # 可能失败的API调用
    pass
```

## 📈 监控和日志

### 日志配置
```python
import logging
from logging.handlers import RotatingFileHandler

# 配置日志
logging.basicConfig(
    level=logging.INFO,
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',
    handlers=[
        # 文件日志（自动轮转）
        RotatingFileHandler(
            'automation.log',
            maxBytes=10*1024*1024,  # 10MB
            backupCount=5
        ),
        # 控制台输出
        logging.StreamHandler()
    ]
)

logger = logging.getLogger(__name__)

# 使用日志
logger.info("开始处理...")
logger.error("发生错误", exc_info=True)
```

### 性能监控
```python
import time
from functools import wraps

def timing(func):
    """计时装饰器"""
    @wraps(func)
    def wrapper(*args, **kwargs):
        start = time.time()
        result = func(*args, **kwargs)
        end = time.time()
        print(f"{func.__name__} 耗时: {end - start:.2f}秒")
        return result
    return wrapper

@timing
def slow_operation():
    # 耗时操作
    pass
```

## 🔧 故障排除

### 常见问题

**1. 认证失败**
```
问题: 401 Unauthorized
解决:
1. 检查用户名和密码
2. 重新运行 python auth_manager.py
3. 检查token是否过期
4. 确认API服务器地址正确
```

**2. 数据库连接失败**
```
问题: Can't connect to MySQL server
解决:
1. 检查数据库服务是否运行
2. 检查连接配置（host, port, user, password）
3. 确认防火墙规则
4. 测试网络连接
```

**3. 速率限制**
```
问题: 429 Too Many Requests
解决:
1. 减少请求频率
2. 增加延迟时间
3. 使用速率限制装饰器
4. 分批处理任务
```

**4. 内存溢出**
```
问题: MemoryError
解决:
1. 分批处理数据
2. 使用生成器而不是列表
3. 及时释放资源
4. 限制并发数量
```

## 📚 依赖管理

### requirements.txt（Python工具）
```txt
# HTTP客户端
requests==2.31.0
cloudscraper==1.2.71

# HTML解析
beautifulsoup4==4.12.2
lxml==4.9.3

# 浏览器自动化
selenium==4.15.2
webdriver-manager==4.0.1

# 数据库
pymysql==1.1.0
mysql-connector-python==8.2.0

# 工具库
python-dotenv==1.0.0
pyyaml==6.0.1
```

### 安装所有依赖
```bash
# 安装Python依赖
pip install -r automation-tools/shared/requirements.txt

# 或使用虚拟环境
python -m venv venv
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate     # Windows
pip install -r requirements.txt
```

## 🔍 SEO分析工具详细使用指南

### SEO Keyword Analyzer 高级功能

#### 环境配置
```bash
# 依赖安装
pip install mysql-connector-python
pip install python-dotenv

# .env.local 文件配置
DB_HOST=your_database_host
DB_PORT=3306
DB_USER=your_username
DB_PASSWORD=your_password
DB_NAME=sitemanager_backup
```

#### 智能分层分析
```bash
# 智能分层分析（推荐）
python seo_analyzer.py --keywords "christmas countdown" --smart --target-count 300

# 传统分析
python seo_analyzer.py --keywords "date calculator,days from today" --min-volume 500
```

#### 分层搜索量阈值
- 第1层: 5000+/月搜索量
- 第2层: 2000+/月搜索量  
- 第3层: 1000+/月搜索量
- 第4层: 500+/月搜索量
- 第5层: 200+/月搜索量
- 第6层: 100+/月搜索量
- 第7层: 50+/月搜索量

### Google Search Console 使用技巧

#### 关键指标解读
- **点击次数**: 从搜索结果获得的点击
- **展示次数**: 搜索结果中的展示次数
- **点击率(CTR)**: 点击次数/展示次数
- **平均排名**: 关键词的平均排名位置

#### 数据分析方法
1. **长尾关键词发现**: 导出所有查询词数据，按展示次数排序
2. **排名分析**: 关注排名在10-20位的关键词
3. **页面分析**: 识别高流量页面和低表现页面
4. **优化机会**: 分析低CTR但高展示的关键词

### 数据分析工作流

#### 第一阶段：初始研究
1. 种子关键词收集（业务核心、竞争对手、用户反馈）
2. 使用SEO Keyword Analyzer进行大规模分析
3. 使用Google Keyword Planner验证搜索量
4. 使用GSC分析现有表现

#### 第二阶段：深度分析
1. 关键词筛选（搜索量100+/月，竞争难度KD<30）
2. 聚类分析识别关键词群体
3. 规划页面结构和内容策略
4. 制定优先级排序

#### 第三阶段：实施优化
1. 创建目标页面
2. 优化页面标题和描述
3. 添加相关内容（FAQ、相关工具链接）
4. 实施内部链接优化

#### 第四阶段：监控跟踪
1. 设置GSC监控和定期检查
2. 分析排名变化和流量增长
3. 调整优化策略
4. 记录成功经验和失败教训

### 最佳实践

#### 工具使用技巧
- **高效使用**: 使用智能分层分析避免数据过载
- **数据质量**: 验证关键词相关性，交叉验证搜索量数据
- **定期更新**: 定期更新关键词数据库和分析参数

#### 常见问题解决
- **数据库连接问题**: 检查网络连接、数据库配置、权限设置
- **API调用失败**: 检查访问令牌、请求格式、权限设置
- **数据不一致**: 了解数据来源差异、使用官方数据验证

---

**更新日期**: 2025-01-16
**基于项目**: youzikuaibao.com.cn, days-from.com
**工具版本**: Python 3.9+, Node.js 18+

