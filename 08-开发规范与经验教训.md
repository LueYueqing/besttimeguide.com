# 开发规范与经验教训

## 📋 文档概述

基于whitescreen.show项目的开发经验，整理出的开发规范、最佳实践和常见错误避免方法。

## 🚨 关键开发原则

### 1. 组件设计原则

#### customContent属性使用规范
```javascript
// ❌ 错误用法
<ColorScreen 
  customContent={<FAQComponent />}  // 不要放FAQ等静态内容
/>

// ✅ 正确用法
<ColorScreen 
  customContent={<DynamicEffect />}  // 只放视觉效果组件
  useCasesContent={<FAQComponent />}  // 静态内容放这里
/>
```

**规则**:
- `customContent` 仅用于视觉效果组件（动画、特效、交互）
- 静态内容（FAQ、说明文字）使用 `useCasesContent`
- 在代码中添加警告注释防止误用

#### 动态效果保护
```javascript
// 在ColorScreen组件中添加警告
export default function ColorScreen({ 
  customContent = null,       // ⚠️ 警告：仅用于视觉效果组件（动画、特效），不要放FAQ或静态内容！
  useCasesContent = null,     // 用于静态内容
  // ...其他props
}) { /* ... */ }
```

### 2. 布局一致性原则

#### 统一布局标准
```css
/* 标准容器宽度 */
.content-container {
  max-width: 1000px;
  margin: 0 auto;
  padding: 0 20px;
}

/* 标准颜色系统 */
:root {
  --primary-color: #12100c;
  --secondary-color: #f5f4f4;
  --border-color: #e0e0e0;
  --text-muted: #666;
}
```

#### 响应式设计规范
```css
/* 移动端优先 */
.container {
  width: 100%;
  padding: 20px;
}

/* 平板端 */
@media (min-width: 768px) {
  .container {
    max-width: 768px;
    margin: 0 auto;
  }
}

/* 桌面端 */
@media (min-width: 1024px) {
  .container {
    max-width: 1000px;
  }
}
```

### 3. 图片资源管理规范

#### 命名规范
```
预览图片: [screen-name]-[size].webp
- white-screen-xs.webp
- white-screen-sm.webp
- white-screen-md.webp
- white-screen-lg.webp
- white-screen-xl.webp

下载图片: [screen-name].png
- white-screen.png
- blue-screen.png
- matrix-screensaver.png
```

#### 图片优化标准
```javascript
// 图片组件标准
<Image
  src="/previews/white-screen-md.webp"
  alt="White screen effect preview"
  width={800}
  height={600}
  loading="lazy"
  priority={false}
/>
```

### 4. 代码质量规范

#### 组件结构标准
```javascript
// 标准组件结构
import React from 'react';
import Head from 'next/head';

const ComponentName = () => {
  // 1. Hooks
  const [state, setState] = useState(null);
  
  // 2. 事件处理函数
  const handleClick = () => {
    // 处理逻辑
  };
  
  // 3. 渲染函数
  return (
    <>
      <Head>
        {/* SEO meta tags */}
      </Head>
      <div className="component-container">
        {/* 组件内容 */}
      </div>
    </>
  );
};

export default ComponentName;
```

#### 错误处理规范
```javascript
// 图片加载错误处理
const handleImageError = (fallbackSrc) => {
  return (e) => {
    e.target.src = fallbackSrc;
    console.warn('图片加载失败，使用备用图片');
  };
};

// API调用错误处理
const fetchData = async () => {
  try {
    const response = await api.getData();
    return response.data;
  } catch (error) {
    console.error('API调用失败:', error);
    return null;
  }
};
```

## 🚨 常见错误与解决方案

### 1. 组件设计错误

#### 错误：将静态内容放入customContent
```javascript
// ❌ 错误
<ColorScreen 
  customContent={<FAQComponent />}
/>

// ✅ 正确
<ColorScreen 
  customContent={<DynamicEffect />}
  useCasesContent={<FAQComponent />}
/>
```

**解决方案**:
- 在代码中添加警告注释
- 创建开发规范文档
- 建立代码审查流程

#### 错误：动态效果被静态图片替换
```javascript
// ❌ 错误 - 移除动态效果
<ColorScreen 
  previewImage="/previews/static-image.webp"
  // 缺少 customContent
/>

// ✅ 正确 - 保留动态效果
<ColorScreen 
  previewImage="/previews/static-image.webp"
  customContent={<DynamicEffect />}
/>
```

**解决方案**:
- 建立动态效果保护清单
- 在关键位置添加警告注释
- 定期检查页面功能完整性

### 2. 布局一致性问题

#### 错误：页面布局不一致
```css
/* ❌ 错误 - 全宽显示 */
.top-color-bar {
  width: 100%;
}

/* ✅ 正确 - 标准容器宽度 */
.top-color-bar {
  max-width: 1000px;
  margin: 0 auto;
  padding: 0 20px;
}
```

**解决方案**:
- 建立统一的布局组件
- 使用标准化的CSS类
- 定期检查页面一致性

#### 错误：颜色系统不统一
```css
/* ❌ 错误 - 使用新颜色 */
.custom-section {
  background-color: #ff0000;  /* 系统中没有的颜色 */
  color: #00ff00;
}

/* ✅ 正确 - 使用标准颜色 */
.custom-section {
  background-color: var(--secondary-color);
  color: var(--text-muted);
}
```

**解决方案**:
- 建立标准颜色系统
- 使用CSS变量
- 避免硬编码颜色值

### 3. 图片资源管理错误

#### 错误：使用过时的图片文件
```javascript
// ❌ 错误 - 使用过时的图片
<img src="/previews/fbi-warning-xs.webp" />

// ✅ 正确 - 使用最新图片
<img src="/previews/fbi-warning-screen-xs.webp" />
```

**解决方案**:
- 建立图片命名规范
- 使用自动化工具生成图片
- 定期检查和更新图片

#### 错误：图片缺少alt标签
```javascript
// ❌ 错误 - 缺少alt标签
<img src="/previews/white-screen.webp" />

// ✅ 正确 - 包含描述性alt标签
<img 
  src="/previews/white-screen.webp" 
  alt="White screen effect preview showing blank white display"
/>
```

**解决方案**:
- 建立图片alt标签规范
- 使用自动化工具生成alt标签
- 在代码审查中检查alt标签

### 4. SEO优化错误

#### 错误：关键词堆砌
```javascript
// ❌ 错误 - 关键词堆砌
<h1>White Screen White Screen Online Free White Screen Generator</h1>

// ✅ 正确 - 自然包含关键词
<h1>White Screen - Free Online Generator | WhiteScreen.show</h1>
```

**解决方案**:
- 建立关键词密度标准（1-2%）
- 使用自然语言写作
- 避免重复关键词

#### 错误：忽略移动端优化
```css
/* ❌ 错误 - 只考虑桌面端 */
.button {
  width: 200px;
  height: 50px;
  font-size: 14px;
}

/* ✅ 正确 - 响应式设计 */
.button {
  width: 100%;
  min-width: 200px;
  height: 50px;
  font-size: clamp(14px, 2vw, 16px);
}

@media (max-width: 768px) {
  .button {
    height: 44px;  /* 触摸友好 */
  }
}
```

**解决方案**:
- 移动端优先设计
- 使用响应式单位
- 测试各种设备尺寸

## 🛠️ 开发工具与自动化

### 1. 代码质量工具

#### ESLint配置
```json
{
  "extends": [
    "next/core-web-vitals",
    "@typescript-eslint/recommended"
  ],
  "rules": {
    "no-console": "warn",
    "no-unused-vars": "error",
    "prefer-const": "error"
  }
}
```

#### Prettier配置
```json
{
  "semi": true,
  "trailingComma": "es5",
  "singleQuote": true,
  "printWidth": 80,
  "tabWidth": 2
}
```

### 2. 自动化工具

#### 预览图生成
```bash
# 生成所有屏幕效果的预览图
cd tools/preview-generator
python batch-generator.js

# 生成单个屏幕效果
python convert_single_image.py white-screen
```

#### Favicon生成
```bash
# 生成所有屏幕效果的Favicon
cd tools/favicon-generator
python generate_screen_favicons.py

# 生成单个Favicon
python generate_screen_favicons.py white-screen
```

### 3. 开发脚本

#### 项目启动脚本
```bash
# 开发环境启动
npm run dev

# 生产环境构建
npm run build

# 代码检查
npm run lint

# 类型检查
npm run type-check
```

#### 部署脚本
```bash
# 自动推送
./auto-push.bat

# 图片转换
./convert_to_webp.py

# Favicon生成
./convert_favicons.bat
```

## 📋 开发检查清单

### 新功能开发前
- [ ] 阅读开发规范文档
- [ ] 检查现有组件结构
- [ ] 确认设计规范
- [ ] 准备测试数据

### 开发过程中
- [ ] 遵循组件设计原则
- [ ] 保持布局一致性
- [ ] 使用标准颜色系统
- [ ] 添加错误处理
- [ ] 编写清晰的注释

### 开发完成后
- [ ] 检查页面功能完整性
- [ ] 验证响应式设计
- [ ] 测试图片加载
- [ ] 检查SEO优化
- [ ] 运行代码检查工具

### 上线前
- [ ] 生成预览图片
- [ ] 生成Favicon
- [ ] 检查所有链接
- [ ] 验证移动端体验
- [ ] 测试加载速度

## 🎯 最佳实践总结

### 1. 代码组织
- 使用清晰的文件夹结构
- 遵循命名规范
- 保持组件单一职责
- 添加必要的注释

### 2. 用户体验
- 移动端优先设计
- 快速加载（<3秒）
- 直观的交互设计
- 一致的视觉风格

### 3. 性能优化
- 图片懒加载
- 代码分割
- 缓存策略
- 压缩资源

### 4. SEO优化
- 语义化HTML
- 结构化数据
- 关键词优化
- 内部链接

### 5. 维护性
- 文档化
- 版本控制
- 自动化工具
- 错误监控

## 📚 相关资源

### 技术文档
- [Next.js官方文档](https://nextjs.org/docs)
- [React官方文档](https://react.dev)
- [Tailwind CSS文档](https://tailwindcss.com/docs)

### 开发工具
- [ESLint配置指南](https://eslint.org/docs/user-guide/configuring)
- [Prettier配置](https://prettier.io/docs/en/configuration.html)
- [TypeScript配置](https://www.typescriptlang.org/docs)

### 设计资源
- [Material Design](https://material.io/design)
- [Figma社区](https://www.figma.com/community)
- [Unsplash图片](https://unsplash.com)

---

**更新日期**: 2025-01-16  
**基于项目**: whitescreen.show  
**适用范围**: 所有Next.js + React项目  
**维护者**: 开发团队  

## 🎉 总结

这套开发规范基于whitescreen.show项目的实战经验，涵盖了：

1. **组件设计原则** - 避免常见的设计错误
2. **布局一致性** - 确保用户体验统一
3. **图片资源管理** - 规范化的资源处理
4. **代码质量** - 可维护的代码标准
5. **自动化工具** - 提高开发效率

遵循这些规范可以避免重复犯错，提高开发效率，确保项目质量。
